import { SpawnOptions } from 'child_process';

export class ServerDb {
    sno:Number;
    server:string;
    server_instance:string;
    environment:string;
    port:number;
    status:string;
    remarks:string;
}
